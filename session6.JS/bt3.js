let numbers = [12, -5, 7, -20, 0, 15, -3, 8];
console.log("Mảng số nguyên:", numbers);
for (let i = 0; i < numbers.length; i++) {
    if (numbers[i] < 0) {
        console.log(" phần tử là số âm:", numbers[i]);
    }
}
